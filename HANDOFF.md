# Nexez — Session Handoff

_Last updated: 2026-06-17 — this session shipped a **public buyer order portal** (anonymous buyers view/track transactions + request refunds/report problems + email "find my orders" magic-link), a **platform admin role** (entitlements god-mode for testing/staff), and — the big thread — **made TEAM COLLABORATION actually work end-to-end**: invites now email the invitee, the access RLS was fixed (it never worked), and **editor-collaborators now INHERIT the page owner's entitlements** (AI/custom-domain/credentials/duplicate + collaborator-aware Settings). Also dropped the broken `supabase db push` CI job. **855 tests · lint · palette · tsc · build**. Prior context still current: Stripe LIVE mode active + validated (real escrow→capture→refund on `acct_1Tiny6IWDbJDFa4l`), Finance dashboard = two-sided marketplace cockpit, monetization enforced end-to-end, launch-readiness audit cleared. **HEAD moves constantly** — Flyger1an / Grok Build / codex push to `main` in parallel; `git fetch` first. **Collaboration BATCH 2 is SHIPPED** (editor-inherits-owner extended to the integration imports + trust-report + analyze-competitor via the new `resolveFeatureOwner` optional-page helper, client callers wired, + deferred hardening a/b/c/d — see Shipped + Pending). **2026-06-17:** demo business rebranded **AxleStrategy → Nexez Agency / nexezagency.com** (copyright safety; form placeholders → neutral "Apex"), **theme default flipped DARK → LIGHT** for human surfaces (agent `/<slug>` pages stay fixed; toggle preserved), and **hover `title` tooltips** added to icon-only buttons (PageCard row actions, shell sign-out, offer-builder, etc.). Then **all transactional email moved to branded react-email templates** (`emails/`, exact brand palette, one shell) rendered by async `build*Email` in `lib/email.tsx`. The migration then **silently broke prod email** and took a long debug to fix (BOTH MCPs — Vercel + Supabase — were down all session): first a serverless **output-tracing gap** made `render()` throw (fixed with `serverExternalPackages` + a plain-HTML render fallback, `10923e0`), then the REAL root cause — pages with **no saved `contact_email`** skip the owner-notify branch (no recipient). Fixed by **`resolveOwnerNotifyEmail` (page contact_email → owner ACCOUNT email)** + inline send at every owner-notify site (`f3127f2`/`7eca6c1`); branded email **verified live in Resend + Gmail**. **Then (continued 2026-06-17, `d196bd3`): the email/refund leftovers cleared —** in-app **PARTIAL refunds** SHIPPED (new `refunded_cents` cumulative ledger → equal-amount partials no longer collide on the Stripe idempotency key), the two ready-but-**UNWIRED emails (Welcome + StripeConnected) got WIRED** via a send-once ledger (`sent_system_emails`), the **4 QA email-test negotiations were cleaned up**, and a billing **price-id guard** was added. 1c (move webhook seller emails inline) was reviewed + deliberately NOT done (`after()` is correct for webhooks). **Then the homepage (nexez.ai) was REDESIGNED + shipped (`636a5a3` → `8071ef9`):** a signature **Agent X-Ray hero** ("Nexez Spa" cluttered-site-vs-clean-agent, draggable on desktop / Human-Agent toggle on mobile) + interactive **Readiness Lab** + streaming **Live agent feed**, in `components/home/` — ported from a designer zip via a diverge/judge Workflow into our glass palette; periwinkle top-glow hero bg; arrows dropped from primary CTAs. See the Shipped entry below. See [[refund-dispute-money-path]] [[email-react-templates]]._

## ⚡ NEW CONTEXT WINDOW START HERE

**State:** deployed, green (**855 tests · lint · palette · tsc · build**). **This session's threads (all SHIPPED + deploy-verified):** (1) **Buyer order portal** — anonymous buyers get a token-keyed `/orders/<token>` (view txn + status + timeline + request-refund/report-problem → seller notified) covering direct checkout AND negotiation; webhook captures `buyer_email` + emails receipts/status; phase 2 email "find my orders" magic-link (signed/expiring, anti-enumeration). (2) **Platform admin role** (`platform_admins` table) — resolves an owner to the top plan tier everywhere (entitlements only, NOT RLS/cross-tenant bypass; not self-grantable); `realestglad@gmail.com` granted. (3) **Team collaboration FIXED + made whole** — invites were a silent DB write (no email) AND the access RLS was self-defeating (the invitee couldn't read the invite that grants them access) → collaboration had NEVER worked; both fixed, then **editors now inherit the page owner's entitlements** via the new `lib/server/page-access.ts` `resolvePageAccess` primitive (AI, custom-domain, credentials, duplicate; collaborator-aware Settings). (4) CI: dropped the perpetually-failing `supabase db push` job (migrations are MCP-applied). All adversarially reviewed (cross-tenant model SOUND). See memory [[buyer-order-portal]] [[platform-admin-role]] [[team-collaboration-flow]].

The agent-to-agent **negotiation + escrow money system is GA-complete and LIVE** — Stripe live keys are set, the owner's real Connect account (`acct_1Tiny6IWDbJDFa4l`) is linked + charges-enabled, and one real low-value negotiation→escrow→capture→refund txn was run end-to-end with the platform `application_fee` (see the live-mode notes in the money section). The **monetization layer is enforced + surfaced end-to-end** (server-side gating, multi-currency, Connect-required), and the **Finance dashboard** is now a full marketplace cockpit (channel split, money-flow, escrow lifecycle, recent-activity ledger). 3-host architecture — **`nexez.ai`** (marketing), **`app.nexez.ai`** (authed app), **`nexez.app`** (agent runtime); agent contract stays on `nexez.app` (`getBaseUrl()`). The big prior threads are **done** — the full launch-readiness audit fix queue, the visual-delight pass, and the dispute/refund/issue-management hardening all shipped + verified this session (refunds reverse the commission, refund/disputes notify the seller, direct-checkout sales are captured + refundable + dispute-tracked via the new `checkout_orders` subsystem, buyers have recourse, support tickets are transaction-linkable). Remaining work is small / user-driven (see Pending): the **authenticated billing smoke** for the create-subscription 500s (likely TEST `STRIPE_PRICE_*` under live keys — the only thing here still needing the owner's login + card); the intentional `pages_public` SECURITY DEFINER advisor; and a few deferred low-perf advisories. (**In-app PARTIAL refunds are now SHIPPED** — see this session's entry below.)

> ⚠️ **DEPLOY-VERIFY RULE (learned the hard way this session):** after any deploy, **`curl` a published `/<slug>` (200) + its `/agent.json` (200) + `agent-pages.json` (pages>0)** — NOT just `login`/`pricing`/gate routes. A `PUBLIC_PAGE_SELECT` change (adding `currency`) without updating the **`pages_public` VIEW** 404'd the entire public/agent surface for ~33 min while auth routes stayed green. **`pages_public` is a curated VIEW (strips offer `rules`) — every column added to `PUBLIC_PAGE_SELECT` must be added to the view too** (CREATE OR REPLACE, new column appended at END). Diagnostic: `x-vercel-cache: MISS, age:0` returning empty/old = the route is erroring, not a cache.

### Shipped this session (newest first — full detail in git log)

**— 2026-06-17 (latest): homepage redesign — Agent X-Ray hero + Readiness Lab + Live feed (`636a5a3` → `8071ef9`) —**
The marketing homepage (`app/page.tsx` on nexez.ai) was rebuilt from a designer's "Nexez X-Ray" handoff (a zip in ~/Downloads; ported into our Liquid Glass palette via a diverge→judge→synthesize **Workflow**, not the lime reference). New `components/home/`:
- **`AgentXray.tsx`** — the signature hero. It dramatizes "humans see a cluttered site; agents see clean structured data" for the demo business **"Nexez Spa"**. **Desktop (md+):** a COMPACT draggable X-ray that sits in the hero's **right column** beside the headline — a real-feeling marketing site (promo bar, busy nav, category chips, **4 vaguely-priced services** — `from $120*`/`pricing varies`/`call for pricing`/`day rates*`, reviews, cookie banner) vs the clean agent layer (firm prices + book/buy, action/schema chips, a structured-fields block); a draggable vertical scanner (clip-path) reveals one over the other; both halves tuned to fill with no dead space. **Mobile (<md):** a single full-width view + a segmented **Human/Agent toggle** (default Agent) in a browser-chrome frame, with a one-shot "derive" scan sweep — no cramped split. Both panes are FIXED device colors (warm paper / `#08080C`-ish dark) that do NOT theme-flip; only the frame chrome flips. Periwinkle accents via `var(--signal)`.
- **`ReadinessLab.tsx`** — 6 signal toggles → score → animated signal→ready ring gauge + verdict + capability list (interactive; verified 64→78 on toggle).
- **`LiveAgentFeed.tsx`** — streaming session log (rotating agents, append/1.4s, keep 7, fixed-dark terminal so tags pop) + attribution tiles + conversions-by-agent bars.
- **Hero layout:** 2-col `lg:grid-cols-[minmax(0,0.95fr)_minmax(440px,0.9fr)]` — eyebrow/h1/copy/CTAs left, X-Ray right; stat ticker full-width below; **periwinkle top-glow background** (`radial-gradient(--signal …), var(--bg)` — blends light + dark; a graphite-hero experiment was tried + discarded). Headline "Where AI agents **buy from you.**" 12-model monogram marquee. **Arrows removed** from "Get started" (nav, MarketingShell) / "Get started free" / "Deploy your agent page". Cuts over cleanly into the existing **Key capabilities → How it works → Simulator → Examples → CTA** (untouched).
- **CSS (`app/globals.css`):** added `nx-pulsedot` / `nx-handle-pulse` / `nx-log-in` / `nx-derive-beam` keyframes (accent glow via `var(--signal)`, reduced-motion-gated).
- **Preview gotcha (already in [[nexez-deploy-verify-loop]]):** to screenshot via the preview tooling, the dev server MUST run on **PORT=3000** — `next.config.ts` `redirects()` bounces `localhost:*` → `127.0.0.1:3000`. 855 tests · lint · palette · tsc · build green throughout; deploy-verified live (new markers present, old 6-card-grid markers gone, agent surface 200). The desktop X-Ray went through several iterations (cramped full-width → diptych "Derive Frame" → final two-column draggable) per owner review.

**— 2026-06-17: email/refund leftovers cleared (`d196bd3`) —**
- **In-app PARTIAL refunds — the deliberately-deferred money path, now SHIPPED.** New cumulative-refunded ledger column **`refunded_cents`** on `agent_negotiations` + `checkout_orders` (migration `20260626000900`; Stripe smallest-unit, backfilled from existing refund metadata). Both refund routes (`/api/orders/refund` + `/api/negotiations/escrow` action `refund`) take an optional partial **`amount` in MAJOR units** of the deal/page currency (omit = full remainder): cap on `(captured − refunded_cents)`, **key Stripe idempotency on the NEW running total** (`refund-<id>-<newTotal>`) so two equal-amount partials get DISTINCT keys — the exact collision the deferral was about (Stripe would otherwise return the cached first refund → silent under-refund). A partial keeps the deal OPEN (`complete`/`paid`, remainder refundable); only a full refund flips to `refunded`. `refund_application_fee: true` stays (Stripe reverses the fee proportionally). The `charge.refunded` webhook reconciles `refunded_cents` to Stripe's authoritative `amount_refunded` (covers out-of-band dashboard partials); Stripe's own ceiling is the final over-refund backstop. Pure tested helper **`lib/refunds.ts`** (`planRefund`/`refundIdempotencyKey`). UI: OrdersPanel + Negotiation card get a partial-amount input (prefilled to the remainder) + "X refunded"/"Refund more" states. See [[refund-dispute-money-path]].
- **Welcome + StripeConnected emails — wired (were branded-but-UNWIRED).** New send-once ledger **`sent_system_emails(owner_id, kind)`** (migration `20260626001000`, service-role only) + **`lib/server/system-email.ts` `sendOnceSystemEmail`** (atomic unique-insert guard; rolls back the claim on send failure so it can retry; race-safe across redeliveries/logins). **WelcomeEmail** fires from the auth callback on a new signup (`created_at < 24h` so existing users aren't backfill-blasted, deferred via `after()`); **StripeConnectedEmail** fires from the `account.updated` webhook on the Connect `charges_enabled` false→true flip. See [[email-react-templates]].
- **Billing create-subscription guardrail (`2` partial):** reject a non-Price-ID value (e.g. a pasted `prod_…` id) up front with actionable copy; the existing "no such price" handler already covers the TEST-id-under-live-keys case. The live authed subscribe smoke stays **user-driven** (needs a real login + card / Vercel env access).
- **1b — cleaned up the 4 QA email-test negotiations** on `gauntlet-negotiation-lab` (+ their messages) once the Supabase MCP was back; left the 6 `GauntletActual` rows + `qa-neg-sim-33`.
- **1c — reviewed, deliberately NOT done:** keeping `after()` for the stripe-webhook + calendly seller emails — inline would block the Stripe 200 on Resend latency (risking a timeout→retry that the event-id ledger then dedupes → email SKIPPED). The real root causes (render-in-serverless + recipient) are already fixed.
- Verified: lint · palette · tsc · **855 tests** · build green; migrations applied to prod (additive, deployed before the code); public/agent surface stayed 200 (no `PUBLIC_PAGE_SELECT`/`pages_public` touch).

**— 2026-06-17: transactional email → branded react-email templates (`a424e39`) + the prod-fix saga (`10923e0` → `7eca6c1`) —**
- All transactional email migrated from hand-written HTML strings to **react-email** (`@react-email/components` + `@react-email/render`, React 19). New **`emails/`**: `theme.ts` (the ONE source of email brand colors — light tokens, signal `#5566F2`, **signalSolid `#4f46e5` for every CTA**, ready/amber/danger), `BrandedEmail.tsx` (shared shell: `<Html>/<Head>/<Preview>`, periwinkle logo-mark + `nexez.` wordmark header, white card, divider'd footer, Outlook MSO ghost-table + explicit width since Outlook ignores CSS max-width), `templates.tsx` (one component per email + branded Welcome/StripeConnected that are READY but UNWIRED — they need a send-once trigger).
- **`lib/email.ts` → `lib/email.tsx`:** `build*Email()` are now **async** (`render(<Template/>)` for HTML; hand-authored plain-text + subjects kept, so the text stays clean + unit-tested). `sendEmail`/`hasEmailEnv` unchanged (Resend REST, `RESEND_API_KEY`-gated). Fixed the inconsistent old CTA colors (green `#10B981` / old-purple `#7C3AED`) → unified periwinkle.
- **Async ripple:** all ~14 call sites `await` the builders. In the **stripe webhook (money path)** every email build+send stays inside `after()` best-effort blocks → a render/send failure can NEVER break Stripe settlement. Tests async; assertions survive (React escapes the same way; CTA URLs land in the button href). Adversarial multi-agent review: 1 confirmed finding (Outlook width) fixed.
- **⚠️ Two prod bugs the migration introduced + their fixes (email was silently DOWN until these):** (1) **serverless output-tracing** dropped a transitive dep of `@react-email/render` → `render()` threw at runtime (passed local build + 836 tests because local has full node_modules) → fixed with **`serverExternalPackages: ['@react-email/components','@react-email/render']`** in `next.config.ts` + a **`renderHtml()` try/catch fallback** (logs + `captureError` + plain-HTML from the text body, so a render failure can never silently kill an email again) — `10923e0`. (2) THE actual recipient bug — **pages with no saved `contact_email` skipped the owner-notify branch** (the settings UI shows the account email by DEFAULT but never persists it; even the gauntlet test page had none) → **`lib/server/owner-email.ts` `resolveOwnerNotifyEmail({contactEmail, ownerId})`** falls back to the owner's account email via `admin.auth.admin.getUserById`. Applied at the negotiation route (`f3127f2`, also moved INLINE/awaited so it doesn't depend on `after()` flushing) + calendly booking + the 4 stripe-webhook seller sends (`7eca6c1`, added `owner_id` to those `pages` selects). **Diagnosed in prod via a temp `[neg-email]` console.log (`62632d9`, since removed) + the user reading the Vercel log** (`hasOwnerEmail:false`) — both MCPs were down. **Lesson: a green local build + tests do NOT prove a server lib renders/sends in the Vercel serverless bundle — always confirm a real email lands (Resend) after deploy.** See [[email-react-templates]].

**— 2026-06-17: platform rebrand (AxleStrategy→Nexez Agency) + LIGHT theme default + icon-button hover tooltips —**
- **Rebrand for copyright safety:** the demo/example business **"Axle Strategy" / axlestrategy.com → "Nexez Agency" / nexezagency.com** across marketing (home BentoGrid + ProductPreview, PhilosophySplit, ConversionProofSections, heroes), the simulator demo page (`lib/agent-simulator.ts`), and the `Reference/*.html` mockups. Form placeholders (the "your business name" / logo-URL fields in settings, LoginForm, ProfileSettings) → neutral **"Apex Plumbing Co." / apexplumbing.com** (don't show the platform's own brand where the user types theirs). Test fixtures → neutral "Apex Advisory". `grep -i axle` is now clean across all shippable files.
- **Theme default DARK → LIGHT (human surfaces only).** `lib/theme.ts` `THEME_NO_FLASH_SCRIPT` + `ThemeToggle.tsx` now default to `light` when there's no stored preference (no-flash, set on `<html>` pre-paint; confirmed in served HTML). The public **agent `/<slug>` pages stay on their fixed clean surface** (`.public-agent-page` re-declares dark) and the **light/dark/system toggle is unchanged** (persists to localStorage). Light mode was already token-driven via the comprehensive globals.css neutral remap (all `text-white`/`bg-black`/`bg-zinc-*`/`bg-white/x` AND the specific arbitrary hexes used → tokens that flip); the only gaps fixed here: added `bg-[#0b0d12]` to the remap, the checkout dark gradient → `--panel`/`--bg-2` token stops, billing `text-[#4B5563]`→`--fg-muted-2`, and the button `active:bg-[#5B21B6]`→`active:opacity-80`. ThemeToggle test updated (defaults to light).
- **Hover tooltips on icon-only buttons** (the "don't make users guess" pass): added `title` (mirroring the existing screen-reader `aria-label`, which alone does NOT show on hover) to PageCard's 7 row actions (edit/settings/test/analytics/preview/copy/delete), PlatformShell sign-out ×2, VisualOfferBuilder drag/remove, LoginForm show/hide-password, settings publish-toggle, integrations "options", CredentialsManager remove, Onboarding/PublishCelebration dismiss, MarketingShell menu. **NOTE:** local visual screenshots weren't feasible — `proxy.ts` 307-redirects bare `localhost` → `127.0.0.1:3000`, so the human surfaces only render with a real `Host` (verify on the live hosts / via `curl -H "Host: nexez.ai"`); design-system tokens make the theme flip mechanical, verified by lint/palette/tsc/836 tests/build + served-HTML light default.

**— 2026-06-16 (earlier): collaboration BATCH 2 — editor-inherits-owner on imports/trust/competitor + deferred hardening —**
- **`lib/server/page-access.ts` `resolveFeatureOwner({pageId?, userId, userEmail, userEmailConfirmedAt?})`:** the OPTIONAL-page companion to `resolvePageAccess`. pageId PRESENT → caller must be owner OR confirmed editor; returns the PAGE owner `{scoped:true}` (route then gates + acts AS the owner via the admin client); denies 403 (stranger/viewer) / 503 (no admin env). pageId ABSENT → page-less create/sandbox: caller self-gates `{scoped:false, ownerId:userId}`. Returned as a discriminated `{ok:true,…} | {ok:false,status}`.
- **Converted 7 feature routes** to gate on the EFFECTIVE OWNER: **integration imports** (calendly/stripe/shopify/square/acuity — `integrations`/Pro) + **trust-report** (`aiFeatures`, deterministic fallback below) + **analyze-competitor** (`aiFeatures`, 402 below). All parse the body first, `resolveFeatureOwner`, then `ownerAllows(scoped ? adminClient : sessionClient, ownerId, feature)`. Imports are stateless re owner data (return offers to the client; the page write is separately authorized in the editor save).
- **Wired the client callers** to send `pageId` where a saved page exists: `usePageEditor` resync, settings/page.tsx resync (calendly/stripe/shopify), `AnalyticsActions` (trust-report), `CompetitorCompare` (matched myPages slug→id). `Importers.tsx`/`CalendlyTool.tsx` are the page-less `/dashboard/tools` sandbox → correctly self-gate (no pageId).
- **Deferred hardening:** (a) `resolvePageAccess` now requires `userEmailConfirmedAt` in the collaborator branch (fail-closed; owner-by-id unaffected) — threaded through all 9 existing callers + the helper; (b) migration `20260626000700` CHECK `team_invites.email = lower(email)`; (c) migration `20260626000800` `nz_enforce_custom_domain_single_owner` trigger — one domain → one owner (a plain UNIQUE index would break the **multi-page custom-domain** feature; same owner reuse stays allowed; the proxy already serves only `custom_domain_verified` domains; UNVERIFIED-stale-claim-blocks-real-owner remains a support-resolvable edge); (d) Settings shows a collaborator/view-only role badge.
- **Tests:** extended `page-access.test.ts` (resolveFeatureOwner + the email-confirmed gate) + NEW gate tests for all 5 imports + reworked trust-report/analyze-competitor tests (stranger-403, collaborator-gates-on-owner-via-admin, self-gate). **836 tests green.** Trigger verified live (cross-owner claim blocked, same-owner reuse allowed). Cross-tenant model held (sole gate, server-resolved ids, viewers 403'd, no owner-data write in imports).

**— 2026-06-16 (earlier): team collaboration made whole — editors inherit the owner (`b375d27`, `a0ef939`) —**
The earlier authZ map found editing a shared page was content-only: every feature route assumed `req.user == page owner` (gate on `user.id` + `.eq('owner_id', user.id)`), so editors were blocked from the owner's premium features, and "Duplicate" cloned into the editor's own Free account.
- **`b375d27` — new security primitive `lib/server/page-access.ts` `resolvePageAccess({pageId, userId, userEmail, requireEditor})`:** authoritatively (service-role) resolves the page's ACTUAL owner + authorizes the caller as owner OR a non-revoked **editor** invitee (exact verified-email `.eq`, no wildcards; viewers rejected). It's the SOLE gate; routes then gate on + act AS the owner via the admin client. Converted: `/api/ai/{suggest,enhance}`, `/api/tools/import-site` (aiFeatures), `/api/custom-domain` + `/api/verify-custom-domain` (customDomain), `/api/credentials`; NEW `/api/pages/duplicate` (clones under the OWNER, per-owner cap). Account-level stays owner-only (api-keys, account webhooks, analytics). **Deliberate scope.**
- **`a0ef939` — collaborator-aware Settings:** the custom-domain UI lives in `/dashboard/[id]/settings` (1965-line client) which editors couldn't reach (`.eq('owner_id',user.id)` load, `usePlan()`=editor's plan, owner-only `page_secrets`). Two endpoints reuse `resolvePageAccess(requireEditor)`: **GET `/api/pages/[id]/settings-context`** → owner's effective plan + role + secrets; **POST `/api/pages/[id]/secrets`** → upsert page_secrets under the owner (whitelisted). Settings now loads by id, takes the owner's plan, routes secret writes through the endpoint.
- Adversarial cross-tenant review (15 agents): **act-as-owner model SOUND** — resolvePageAccess sole gate, server-resolved ids, viewers 403'd, whitelist blocks injection, self-grant closed. App-layer only, no migration. See [[team-collaboration-flow]].

**— 2026-06-16: team collaboration — invites + the access RLS FIXED (`a4b5648`, `39fa78d`) —** Collaboration had **never worked end-to-end** (two compounding bugs, both fixed + DB-verified):
- **`a4b5648`** — invites were a SILENT client-side DB insert (no email) → the teammate was never told to sign up. New **`POST /api/team/invite`** (owner-session insert so RLS + the Scale/admin plan-gate still apply; gate→402) emails the invitee (`buildTeamInviteEmail`) a link stressing they must sign in/up with that EXACT email. Anti-abuse dedup (a live invite → "already invited", no re-send) + `emailed` flag.
- **`39fa78d` (migration `20260626000600`)** — the access RLS was **self-defeating**: the page-access policy checks `exists(select 1 from team_invites ...)`, but `team_invites` RLS only let the OWNER read invites → in the INVITEE's session the subquery saw nothing → 0 pages. Fix: SELECT-only policy `"invitees read own invites"` (own verified email). Verified via `set local request.jwt.claims` simulation: pages visible **0 → 37**, editable as editor.

**— 2026-06-16: platform admin role (`24fd370`, migration `20260626000500`) —** No admin concept existed. New `platform_admins` table → resolves an owner to the TOP plan tier everywhere at BOTH layers (TS `getOwnerPlanId`→enterprise; SQL `owner_plan_rank`→4, `plan_published_page_limit`→max). **Entitlements only — NOT an RLS/cross-tenant bypass; NOT self-grantable** (writes service-role only, own-row SELECT). `realestglad@gmail.com` granted (DB-verified). Adversarially reviewed. See [[platform-admin-role]].

**— 2026-06-16: buyer order portal — phase 1 + 2 (`bb804ee`, `2dbc61e`; migration `20260626000400`) —** Closed the gap that the only buyer recourse was a `mailto:`. Anonymous buyers get a token-keyed **`/orders/<token>`** (view txn + status + timeline + **request refund / report problem** → notifies the seller; never moves money) covering **both** direct checkout (`access_token`) and negotiation (`status_token`) buyers; webhook captures `buyer_email` + emails a receipt on purchase + status updates on refund/dispute; seller-side `BuyerRequestsPanel` in Finance. **Phase 2 (`2dbc61e`):** email **"find my orders"** magic-link — `/orders` form → `POST /api/order-portal/lookup` (always-neutral response = anti-enumeration; IP + per-email rate limits) → HMAC-signed, 24h-expiring link → `/orders/find/<token>` lists all that email's orders. Two adversarial review rounds; PostgREST `*`-wildcard escape bug caught + fixed. New `order_requests` table (service-role writes, owner-read RLS). See [[buyer-order-portal]].

**— 2026-06-16: CI — dropped the `supabase db push` deploy job (`dc8327f`) —** The `deploy-migrations` job perpetually failed (the `schema_migrations` history drifts from the files since migrations are MCP-applied; it also used the Node-deprecated `supabase/setup-cli@v1`). Removed it; kept the `migration-sanity` filename guard. A *working* `db push` here would be a footgun vs MCP-applied state. **No platform impact** (the live DB was always migrated via MCP).

**— 2026-06-16 (earlier): dispute/refund/issue-management hardening (`dedb586`→`b7af2ea`) —**
Closed the gaps from the "is dispute/refund airtight?" audit (it wasn't). All adversarially reviewed + live.
- **`dedb586` (#1+#4):** escrow refund now sets **`refund_application_fee: true`** (was missing → seller ate the full refund while Nexez kept its commission); refunds + disputes now **email the seller** (`buildMoneyEventEmail`: refund / dispute_opened [urges responding before the Stripe evidence deadline] / dispute_closed) from the webhook reversal handler.
- **`5efdc1d` (#2 — full subsystem):** direct (non-negotiation) checkout had NO in-app refund + disputes on it were **silently dropped** (webhook matched negotiations only). New **`checkout_orders`** table (migration `20260626000200`; owner-SELECT-only RLS, least-privilege grants — revoke Supabase defaults, re-grant authed SELECT; writes service-role). Webhook captures the order on `checkout.session.completed` (`nexez_source=agent_checkout`, with PI) + a "sale" email; the reversal handler now **matches `checkout_orders` by PI** (refund/dispute → status + notify). New **`/api/orders/refund`** (owner-auth, RLS, fee-reversal, idempotency-keyed). **`OrdersPanel`** in Finance (full-refund button).
- **`5d7ef80` (#3):** buyer recourse on the checkout success page — a "Contact the seller" mailto (from `pages_public.contact_email`, prefilled with the Stripe session) so a buyer with an issue has a path (seller then refunds from Finance).
- **`b7af2ea` (#6 + webhook partial guard):** support tickets can be **transaction-linked** (migration `20260626000300`: `transaction` category + nullable `reference`; SupportDesk field). Webhook now keeps a row **open on an out-of-band PARTIAL** refund (records `partial_refund`, doesn't mark fully refunded).
- **#5 in-app PARTIAL refunds — DEFERRED (NOT shipped).** The review caught a real money bug: keying refund idempotency on `(id, amount)` makes two equal-amount partials collide → Stripe returns the cached first refund → buyer silently under-refunded. **Safe partials need a cumulative-refunded ledger** (track refunded-to-date, cap on remainder, key on the running total). In-app refunds stay **full-only**; out-of-band partials are handled correctly by the webhook guard. **Migrations applied: `20260626000200`, `20260626000300`.**

**— 2026-06-16 (latest): visual-delight pass (`d027b91`) —**
- **Encouraging empty states.** New reusable **`components/EmptyState.tsx`** (icon-in-circle + warm copy + optional CTAs; server-safe, on-palette) modeled on the Negotiations/Onboarding pattern. Wired into the flat ones: Analytics `EmptyPanel` (subtle Sparkles icon + next-step hints), Finance `EmptyFinance`, API keys, Team invites.
- **Celebration moments.** New **`components/editor/PublishCelebration.tsx`** — one-time "🎉 Your page is live" banner on the editor's `?created=1` (NOT drafts; reads + strips params via replaceState), with View-live/Copy-link/Set-up-payouts CTAs. `ReadinessChecklist` flips to a celebratory "Agent-ready 🎉" state at 100% (gradient bar + popping check).
- **Subtle platform-wide motion** (CSS-only, `animation-fill-mode:both` so content is NEVER hidden if JS fails; all under `@media (prefers-reduced-motion:no-preference)`). `globals.css` adds `nx-rise`/`nx-rise-stagger`/`nx-celebrate`(`-pop`) keyframes+utilities. Applied: empty states rise in; Finance + dashboard-overview KPI grids enter staggered. **Verified live in a throwaway preview route (screenshots).** Adversarially reviewed (empty states + celebrations SAFE). **KPI count-ups were intentionally NOT used** — the global `.kpi-num` class couples a `font-size:2.2rem` override AND its rAF text-mutation fights React's client-side data refresh on the dashboard; use a React `<CountUp>` if count-ups are wanted later.

**— 2026-06-16 (later): money-correctness + email-reach + create checklist + launch audit —**
- **Negotiation zero-decimal charge fix (`1f4a758`) — real money bug.** `amount_cents` is stored major×100 regardless of currency but the pay route passed it RAW as the Stripe `unit_amount` → JPY/KRW were charged **100×**. New `lib/currency.ts` `minorToStripeAmount()` (keys off `isZeroDecimalCurrency`, like direct checkout); pay route charge + `application_fee` + idempotency + fingerprint all converted; **the webhook settle amount-match uses the SAME conversion** (else zero-decimal deals never match → never settle). USD math unchanged. PLUS `createNewNegotiation` now sets `currency: normalizeCurrency(page.currency)` (was relying on the DB default `'usd'` → non-USD pages mis-recorded + charged in USD).
- **Seller email on escrow funding (`1f4a758`)** — closes the ROADMAP "email reach → Stripe webhook" gap. `buildEscrowFundedEmail` (held/captured, currency-aware), gated on `RESEND`, fired via `after()` so it never blocks the webhook 200. Mirrors the Calendly booking email (owner = `page.contact_email`).
- **`/create` "what's still missing" readiness checklist (`21f8d77`)** — the long-skipped magic-touch item (parallel WIP had landed). `getReadinessCriteria()` is now the single source of truth (id/label/met/hint × 11), `getReadinessScore()` derived from it (output unchanged). New `components/ReadinessChecklist.tsx`; rendered in step 3. Also fixed: `industry` was collected but omitted from the score input → % was under-counted, could never hit 100.
- **`lib/ai-optimize` split:** audited, found ALREADY correctly split (`lib/ai-optimize.ts` secret-free; LLM isolated in `lib/ai-optimize-llm.ts` w/ `server-only`); the 3 fail-open paths are intentional + safe in prod. **No change** (a guard would be semantically false). Removed from the deferred list.
- **Launch-readiness audit (6 parallel agents + live DB/advisor checks)** — overall **85/100**, no Critical, **one real High (H1)**: any signed-up user can read every published page's owner-private offer `rules` (negotiation floors) via base `pages`. See the audit section below.

**— 2026-06-16: Stripe LIVE cutover + money-path hardening + Finance rebuild + UX/audit —**
- **Stripe LIVE mode — cutover DONE + validated end-to-end.** Owner set live keys; the real Connect account `acct_1Tiny6IWDbJDFa4l` is linked + charges-enabled. Ran a real $2 negotiation→agreement(auto)→live Checkout→capture→refund on the connected account with the platform `application_fee`. **Four real money-path bugs this surfaced + fixed:**
  - **`b458d5d`** — Connect account id was silently dropped: `/api/billing/connect` wrote it with the user-**session** client, but `billing_subscriptions` RLS allows owners SELECT only (no insert/update), so the write was rejected and the route still 200'd → owner "approved" in Stripe but forever "not connected", and `account.updated` (matches by acct id) could never sync. Now persists via `createAdminClient()` (the webhook/portal pattern) + surfaces failures. Same latent bug fixed in `/api/billing/create-subscription`. **RLS RULE: all `billing_subscriptions` writes use service-role.**
  - **`b361fb5`** — (1) Connect needs a SEPARATE webhook endpoint for **connected-account** events with its OWN signing secret; the handler verified one secret, so the escrow `checkout.session.completed` (a connected event) failed verification. Now verifies against `STRIPE_WEBHOOK_SECRET` **+ `STRIPE_WEBHOOK_SECRET_CONNECT`** (multi-secret loop). (2) `reconcile-escrow` cron only scanned negotiations with a stored PI, so a paid session whose webhook never landed was invisible → added a **backfill pass** (resolve the session on the connected acct → store PI → heal). This recovered the live $2 deal.
  - **`7b86047`** — owner negotiation actions `/api/negotiations/{escrow,transition}` were canonicalized to the agent-runtime host (under `/api/negotiations`) → dashboard POST hit a cross-origin 308 ("Load failed"). Pinned them to `APP_HOST` (added to `APP_API_PREFIXES`); agent/buyer routes (`/api/negotiations`, `/pay`, `/status`) stay on the runtime.
- **Finance dashboard → true marketplace cockpit (`ce1ff29`).** Period-over-period KPI delta chips + insight band; "Where the money goes" two-leg flow bar; **direct-checkout vs negotiated-escrow channel cards** + share bar; **escrow lifecycle** strip (held→captured→reversed + reversal rate); **unified recent-activity ledger** (both channels). New pure helpers in `lib/finance-analytics.ts` (`rollupNegotiationsByCurrency`/`getReversalRate`/`buildMarketplaceLedger`) + `getNextPayout`. Strict money honesty (never sum across currencies/channels; intent vs cash; est. fee). Designed + adversarially verified via workflows.
- **Magic-touch backlog cleared (`41ed58d`):** Analytics period-over-period KPI deltas + insight band (`previousPeriodBounds`/`pctDelta`); Pages-manager search+sort; open-negotiations **nav badge** in `PlatformShell` + the dead `mobile` flag now orders the mobile strip; "win the query" simulator framing. **Integrations DB-backed status (`1bba97d`):** new `user_integrations` table (migration `20260624000000`) replaces the per-browser localStorage status. **Gate-copy `upgradeBody()` (`22e8b32`).**
- **Negotiation `requested_terms` rendered readable (`cdbe06f`)** — `humanizeTermKey`/`formatRequestedTerms` in `lib/negotiations.ts` (no more raw `JSON.stringify` in the inbox/receipt).
- **`/create` draft-fallback on the publish limit (`1a7e621` → inline `29fbf3b`).** Landed the WIP, reconciled onto the inline-error flow + upgraded from `window.confirm` to an inline "Save as draft" + "Upgrade" action (`handleSubmit({asDraft})`). Fixed a stale `nexez.vercel.app/<slug>` preview leak → `AGENT_RUNTIME_HOST`.
- **Copy/audit polish:** mobile keyboard-hint hiding (`41bc39e`); trimmed redundant platform-fee mentions + the stale Vercel metadata fallback (`f757e0a`); **launch-readiness audit (`4ecb57a`)** — cleared the 2 collaborator-policy init-plan WARNs + the grandfather no-policy INFO (migration `20260625000000`), fixed the standalone `tsc` gate (excluded stale `.next/dev` types), refreshed E2E simulator copy, `npm audit → 0` (moved `shadcn` to devDeps — it pulled a HIGH `hono` + a moderate `js-yaml`), corrected ROADMAP/docs drift.

**— 2026-06-15: monetization enforcement + Finance + multi-currency + UX polish —**
- **SEV1 fix (`a5d81ad`): `pages_public` view was missing `currency`** → the multi-currency commit (which added `currency` to `PUBLIC_PAGE_SELECT`) 404'd every public/agent route for ~33 min. Recreated the view with `currency` (migration `20260623010000`); public surface verified back to 200. **See the deploy-verify rule above.**
- **Marketplace Finance dashboard (`f45111c`) + magic-touch UX pass (`ca2403a`,`dbc07ae`,`8354820`,`a276ad6`).** New **`/dashboard/finance`** (nav: Wallet, between Negotiations & Billing): per **settlement currency** (never summed across currencies) — GMV, net-to-seller vs Nexez commission (take-rate), AOV, escrow held, revenue trend, **live Stripe Connect payouts/balance**, per-currency table, top offers by revenue, subscription cost separated. Pure helpers `lib/finance-analytics.ts` (+test) + server-only `lib/server/connect-finance.ts`. UX touches across home hero (agent-commerce/get-paid story), pricing (cumulative tiers), create funnel, checkout (honest "pay on Stripe" panel), Overview/negotiations empty states, ⌘K page search, Billing "how money flows" band + commission-savings + connect-payout nudge, enriched `agent-pages.json` (price/currency/readiness). **Designed via a 77-agent design+sweep; 57 verified touches, ~16 shipped, rest tracked in Pending.**
- **Multi-currency checkout (`fde7d8d` + the SEV1 follow-up).** `pages.currency` column + selector; `lib/currency.ts` `toStripeAmount()` is **zero-decimal-safe** (JPY/KRW not ×100 — the load-bearing money math, unit-tested); checkout charges in the page currency; buyer checkout page + JSON-LD + analytics revenue all currency-aware (`getRevenueCurrency`/`formatCurrencyAmount`). **Known follow-up:** cross-currency revenue *rollup* is per-currency (single-currency workspaces correct; mixed = approximate, currency recorded on events for a future per-currency breakdown).
- **No-Connect money block (`ab82165`).** A card charge now **requires the seller's Stripe Connect account** — never routes a sale into the platform account (payout/money-transmission liability). No Connect → falls back to the offer's external link (checkout) or 409 (negotiation pay). Onboarding Connect step relabeled "Required to accept card payments".
- **Commission is status-aware (`4881dbe`).** Both commission paths route through `getOwnerPlanId` (live-status only) — a canceled/incomplete `pro` row no longer keeps the 6% rate (reverts to Free 15%). `LIVE_STATUSES` is now a documented shared export.
- **Plan-based feature gating — fully enforced server-side (the big one).** The `lib/billing` catalog (`PLAN_FEATURES`/`FEATURE_MIN_RANK`/limits/commission ladder) was previously wired NOWHERE. Now live across ~15 features via `lib/server/plan.ts` (`getOwnerPlanId`/`ownerAllows`) + `components/billing/{PlanGate,PlanProvider}` (UI teasers): AI routes (Launch+), webhooks/API-keys/integrations/negotiation/analyticsHistory (Pro+), custom-domain (Launch+ boolean **and** per-plan count), team-collaboration (Scale+), white-label/badge (render-time). **Hard chokepoints are DB triggers** (client writes are bypassable): `trg_enforce_published_page_limit` (grandfather-friendly: `max(plan_limit, baseline)`) + `trg_enforce_team_collaboration` (via `owner_plan_rank()`). v1 API rechecks `apiAccess` every request (revokes on downgrade). Agent manifests (agent.json/mcp/llms.txt) honor the negotiation gate so agents never hit a 403 dead-end. Two adversarial review workflows ran over the work; every confirmed finding fixed. **Imports = Pro, but manual entry + CSV stay free** (the anonymous `/create` taste). Commission ladder LIVE: Free 15 · Launch 8 · Pro 6 · Scale 4 · Ent 2. Page limits: Free 1 · Launch 3 · Pro 25 · Scale 100 · Ent ∞.

**— 2026-06-12 and earlier —**
- **Liquid Glass design system — bridge-first integration (IN PROGRESS).** Chosen approach: re-skin platform-wide via tokens/component classes, then upgrade signature surfaces one at a time (verify each in both themes). Source-of-record committed: `DESIGN_SYSTEM.md` (the spec + mobile doctrine), `nexez-design-system.css`, `nexez-fx.js`, `nexez-logomark.svg`, `Reference/nexez-1..7.html` (visual reference). See the **Design system** section below.
  - **Foundation (`360443c`):** fonts Geist → **Schibsted Grotesk / Instrument Sans / JetBrains Mono** (next/font; `--font-*` repointed so Tailwind utilities + `@layer base` pick them up); a **"Liquid Glass bridge" appended to `app/globals.css`** (cascade-wins, coexists with the existing dark/light theme) — design-system tokens for both themes (keyed on `html.light`, the app's class toggle), brand accent **purple → periwinkle (`--signal`)** + teal/amber, `.btn-primary`/`.btn-secondary` refreshed to the glass recipe (sheen + cursor specular + spring), and the new vocabulary (`.btn`/`.glassnav`/`.chip`/`.eyebrow`/`.kpi-num`/`.lede`/`.wrap`/`.display`/…); **`components/DesignSystemFx.tsx`** (React-adapted motion: cursor specular + scroll reveals + KPI counters + ring draw; **omits** the h1 word-stagger + the cloneNode mobile-nav — those fight React and are rebuilt in React per surface); **`NexezLogo`** → the prism-diagonal N mark.
  - **Marketing nav → `.glassnav` (`1582864`):** floating frosted pill (live on every `nexez.ai` surface via `MarketingShell`) + a **React-state mobile menu** (☰ → opaque `.navsheet`, Escape + outside-click close) that honors the hard rule **"never ship a breakpoint where navigation disappears"** — links hide only under `.has-menu` (React adds it post-hydration; no-JS keeps the link row visible as a scrollable strip); Sign-in folds into the sheet ≤480px. Verified dark + light in-browser.
  - **Homepage content (`0cb7ac9`):** `.nx-accent-text` → single periwinkle (rule 2: no multicolor gradient text); `.nx-tile` cards → glass frost + prism hairline + lift. Verified in-browser.
- **3-host rearchitecture (`669c05d`, owner) + multi-agent audit + fixes (`f5b092d`).** The 2-domain split grew a third host: **`app.nexez.ai`** (auth moved here from `nexez.app`), with a **shared `.nexez.ai` session cookie** (`utils/supabase/cookie-options.ts`) so marketing+app share auth — which let the old `nx_authed` ping hack be removed. Audit found no critical/high; fixes: **the proxy custom-domain resolver was reading base `pages` with the anon key (SELECT revoked) → repointed to `pages_public`** (4 QA custom domains were silently failing to resolve); `getRequestBaseUrl` host-sanitization + **`Vary: x-forwarded-host`** on the cached agent artifacts (cache-poisoning hardening); `canonicalHostFor` unlisted-`/api/*` now defaults to the **private app host** (fail-safe); `/api/auth/ping` deleted (dead). Agent contract, money path, and prior hardening all held. **Cutover is DONE** — `app.nexez.ai` attached in Vercel + Supabase Auth redirect allowlist updated; verified live.
- **Auth + security hardening (`22039d7`/`c0e4ed6`/`b10f7f5`/`0bcae92`):** open-redirect guard (`lib/safe-redirect.ts`) on `/auth/callback` + `/login`; rate-limit + auth/opt-in on public LLM-cost endpoints; the **shared-store rate limiter** (`rateLimitShared`, Upstash/KV REST) — **now ACTIVE in prod** (owner set `UPSTASH_REDIS_REST_URL`/`TOKEN`; verified a globally-coherent cap across instances); `/login` bounces already-authed users; scheduling-link host allowlist; `agent_memory` "Public" warning.
- **Earlier this session:** the original `nexez.ai`↔`nexez.app` domain split (core+chrome), the anon-`pages` exposure fix (redacted **`pages_public`** view + revoked anon SELECT, migrations `20260617000000`+`…010000`), Burst 3 (async negotiation decision + layered rate limits + `/dashboard/negotiations/metrics`), Bursts 1&2 (the escrow money rails). (Details in git log.)

### Launch-readiness audit (2026-06-16, `1f4a758`) — overall **85/100**
Ran 6 parallel read-only audit agents + live DB/advisor verification. Build health: lint · palette · tsc(0) · 627 tests · build(0). Sub-scores: Money **91** · Product/UX **90** · Agent-contract **88** · Feature-gating **88** · Security **78** · Data-integrity **78**. **No Critical findings; no money-loss bugs.**
> ✅ **RESOLVED (later 2026-06-16, `c0165fa` + `ea2a287`):** H1 + all 3 Mediums (importer SSRF, `charges_enabled` pre-flight, outbound-dispatch re-gate) fixed, deployed, and verified — the four are listed below (with their original detail) for reference. H1 verified at the DB layer: a random authenticated user now reads **0** base pages (was 302); anon public surface still 200; security advisors unchanged (1 intentional `pages_public` ERROR). **Note:** the `pages` SELECT multiple-permissive perf WARN did NOT fully clear — it dropped from 3 policies to 2 (owner + collaborator remain; the leaky public one is gone), and 2 OR'd access-control policies still trip the (accepted, low-perf) advisor. The **Low batch is now ALSO fixed + deployed + verified** (`f15cfc5` + `e39b2b7`, 633 tests): auth-callback error feedback, escrow-`cancel` guard, both unchecked Connect/billing writes, money DB constraints (migration `20260626000100`, applied — `CHECK amount_cents>0` + partial `UNIQUE` on `stripe_payment_intent_id`), `currency` on per-page agent.json/mcp/llms.txt + JSON-LD `priceCurrency` (verified live: 13x currency in agent.json, 12x priceCurrency on the page), and a **CI parity guard** (`lib/__tests__/pages-public-parity.test.ts`) that fails the build if `PUBLIC_PAGE_SELECT` drifts from the `pages_public` view. **The DR fresh-rebuild gap is now FIXED** (`0000_create_pages_base_table.sql`, verified the chain replays to the live schema). **Only the user-driven authenticated billing smoke remains** from the launch audit (plus the cosmetic applied-vs-disk migration-history drift, which doesn't affect prod).

Actionable findings (fix queue, highest first):
- **🔴 H1 (SECURITY, confirmed vs live DB) — cross-tenant leak of owner-private offer `rules` to ANY authenticated user.** The RLS policy `"Public can read published pages"` (`USING is_published=true`, NOT owner-scoped) applies to `{anon, authenticated}`; `anon`'s base-`pages` SELECT was revoked (→ forced through `pages_public`, good) but **`authenticated` still holds SELECT**. So any signed-up user can `GET /rest/v1/pages?is_published=eq.true&select=products,services` and read every seller's `rules.minPrice` (negotiation floor) — the exact field `pages_public` strips. Defeats the stated privacy boundary; trivially exploitable (open signup, public anon key). **Fix (small, verified-safe):** scope that policy to `anon` only (drop `authenticated`), AND repoint the one authenticated cross-owner base read — `app/api/analyze-competitor/route.ts:48` (uses `PUBLIC_PAGE_SELECT`) — to `pages_public`. Owner/collaborator reads keep working via their own policies; simulator reads own pages only. Side benefit: clears the `pages` multiple-permissive-policy perf WARN.
- **🟠 M (SECURITY) — redirect-SSRF in importer aux fetches.** `lib/importer.ts:587` (`robots.txt`) + `:622` (Shopify `products.json`) use raw `fetch()` (default `redirect:'follow'`) and don't re-validate redirect hops, bypassing the hardened `safeFetch` the rest of the importer uses. A public host 302'ing to `169.254.169.254`/internal would be followed. Route both through `safeFetch`/`fetchTextSafe`.
- **🟠 M (MONEY/FUNNEL) — no `charges_enabled` pre-flight.** `pay/route.ts:118` + `checkout/route.ts:142` route a charge whenever a `stripe_connect_account_id` exists, never checking `stripe_connect_charges_enabled`/`details_submitted` (synced by `account.updated`). An owner mid-onboarding creates dead-end Checkout sessions (no mis-settle, just a confusing fail). Gate on `charges_enabled` + fall back to "finish connecting payouts".
- **🟠 M (GATING) — outbound-webhook DISPATCH not re-checked on downgrade.** Creation is 402-gated, but `fireOwnerOutboundWebhooks` (`lib/server/outbound-webhooks.ts:32`) + `lib/server/log-checkout-event.ts:58` deliver from existing `active=true` rows with no plan re-check → a Pro owner who downgrades keeps receiving deliveries. Add an `ownerAllows(...,'outboundWebhooks')` guard before dispatch.
- **🟠 M (DATA/DR) — migration file set can't rebuild a fresh DB + applied-vs-disk drift.** No `CREATE TABLE public.pages` exists in `supabase/migrations/**` (first migration `0001` ALTERs it); the base schema was bootstrapped out-of-band, and the "clean migration history" reorg renamed files so on-disk order ≠ applied order. **Prod is correct**; this is a disaster-recovery / new-env reproducibility risk. Add a `0000_create_pages` (idempotent) or document the bootstrap. **→ FIXED: `0000_create_pages_base_table.sql` creates the original columns (id/name/slug/is_published/created_at) so the idempotent chain rebuilds the rest; verified vs live (cols 6-39 all migration-added; no drop/rename ops) + on the real engine (rolled-back txn). NO-OP on prod (IF NOT EXISTS). The applied-vs-disk history drift remains — cosmetic; prod schema stays authoritative via MCP, not worth touching the history table.**
- **🟡 Low (hardening, batch when convenient):** unchecked writes at `webhooks/stripe/route.ts:298` (Connect sync returns 200 on failed update) + `lib/server/sync-checkout-session.ts:40` (swallowed upsert error) — the Connect-id-bug class; add DB `CHECK (amount_cents > 0)` + partial `UNIQUE` on `stripe_payment_intent_id`; escrow `cancel` lacks a `status='held'` precondition (`escrow/route.ts:104`); auth-callback swallows code-exchange errors with no user feedback (`app/auth/callback/route.ts:15` + read `?error=` in LoginForm); per-page agent.json/mcp.json/llms.txt omit `currency` (bulk index has it) + public-page JSON-LD `Offer` lacks `priceCurrency`; add a CI parity guard asserting `PUBLIC_PAGE_SELECT ⊆ pages_public` columns (the SEV1 coupling has no automated guard). **→ ALL FIXED + deployed (`f15cfc5` + `e39b2b7`); CI guard = `lib/__tests__/pages-public-parity.test.ts`.**
- **Adversarially re-checked + CONFIRMED SAFE:** money state machine (DB triggers real, search_path pinned, exactly-once webhook idempotency + self-heal, reconcile backfill), feature gating (all server-side + DB-trigger chokepoints; status-aware plan; grandfather not abusable), `pages_public` strips `rules` for anon, secrets clean (no live keys in the public repo), rate-limit key not IP-spoofable, cron fails CLOSED in prod, fail-open paths safe (service-role always set in prod).

### Pending
- **✅ Email leftovers — ALL CLEARED (this session, `d196bd3`).** (1) `WelcomeEmail` + `StripeConnectedEmail` are now **WIRED** via the `sent_system_emails` send-once ledger + `sendOnceSystemEmail` (Welcome on first-signup auth callback; StripeConnected on the Connect `charges_enabled` flip). (2) The 4 QA test negotiations on `gauntlet-negotiation-lab` were **deleted** (the `qa-*` labels were in the message `contact` field, not `buyer_agent`; `buyer_agent` showed "Unknown Agent"). (3) `after()`-flush — reviewed + **kept** for the webhook/calendly seller emails (inline would block the Stripe 200 on Resend latency → timeout→retry→ledger-dedupe → email SKIPPED; `after()` is correct). [[email-react-templates]]
- **✅ Collaboration BATCH 2 — SHIPPED (this session).** Extended editor-inherits-owner to the integration imports (calendly/stripe/shopify/square/acuity), trust-report, and analyze-competitor via the new `resolveFeatureOwner` optional-page helper (`pageId` optional → self-gate when absent); wired the client callers; tests + live trigger check + cross-tenant review. See the Shipped entry above + memory [[team-collaboration-flow]].
- **✅ Collaboration deferred hardening (a/b/c/d) — SHIPPED (this session).** (a) email-confirmed gate in `resolvePageAccess` (fail-closed collaborator branch); (b) `team_invites.email` lowercase CHECK (`20260626000700`); (c) `custom_domain` one-domain→one-owner trigger (`20260626000800`) — chose a trigger over a UNIQUE index to preserve multi-page custom domains; (d) Settings role badge. **Remaining (c) follow-up:** an UNVERIFIED stale custom_domain claim still blocks a later real owner (support-resolvable) — full reclaim/expiry (or a `custom_domain_verified`-aware reassignment) is the separate, larger piece. [[team-collaboration-flow]]
- **✅ H1 + all 3 audit Mediums FIXED + deployed (later 2026-06-16, `c0165fa` + `ea2a287`).** H1: migration `20260626000000` dropped the leaky policy + repointed `analyze-competitor` to `pages_public` (DB-verified: random authed user reads 0 base pages; anon surface 200). Also shipped: importer SSRF (`safeFetch`), `charges_enabled` pre-flight (both charge routes; live seller has it `true` so unaffected), outbound-webhook dispatch re-gate (all auto-dispatch paths). 629 tests · lint · palette · tsc · build green; adversarially reviewed. **Remaining = Low hardening only** (audit §Low: auth-callback feedback, money DB `CHECK`/`UNIQUE` constraints, JSON-LD `priceCurrency` + per-manifest `currency`, the two unchecked Connect-sync writes, a CI `PUBLIC_PAGE_SELECT ⊆ pages_public` guard) + the **DR migration-rebuild gap** (no `CREATE TABLE pages`) + the user-driven billing smoke.
- **Authenticated billing smoke (USER-DRIVEN — highest pre-scale priority).** Prod logs showed a couple of `POST /api/billing/create-subscription` 500s. The RLS silent-write cause is fixed (`b458d5d`); the most likely remaining cause is **`STRIPE_PRICE_*` env vars holding TEST price IDs while live keys are active** → Stripe "no such price" → 500. The route already surfaces that with actionable copy, and `d196bd3` added a `price_`-prefix guard for a pasted `prod_…` id. **Still needs the owner:** verify the plan Price IDs in Vercel are live (`price_…`) and do one authed subscribe + a cross-host auth smoke (login on `app.nexez.ai`, dashboard, sign-out). Can't be driven from here (no password / live card / Vercel env access).
- **✅ In-app PARTIAL refunds — SHIPPED (this session, `d196bd3`, migration `20260626000900`).** The cumulative-refunded ledger (`refunded_cents`) is in: both refund routes cap on the remainder + key idempotency on the running total (equal partials → distinct keys), a partial keeps the deal open, the webhook reconciles to Stripe's authoritative `amount_refunded`, and the UI got partial-amount inputs. **Not yet exercised e2e on a live captured deal** — a good user-driven follow-up (refund part of a real deal, confirm the remainder stays refundable). See memory [[refund-dispute-money-path]] + this session's Shipped entry.
- **Deferred advisors (documented, accepted for launch — `4ecb57a` updated ROADMAP to match):**
  - **`pages_public` SECURITY DEFINER (the 1 remaining ERROR) — intentional + safe.** It's the anon-readable projection that strips owner-only offer `rules`, so anon never gets direct SELECT on base `pages`. Flipping to `security_invoker` needs `rules` moved to an owner-only table + anon column grants (touches every `rules` reader; risks the public-surface SEV1). It IS the security boundary, not a leak — only do as scoped, carefully-tested work.
  - **Perf (low):** 3 multiple-permissive policies (`pages` SELECT/UPDATE, `negotiation_messages` INSERT — functionally correct OR'd policies; consolidating risks access-control bugs), a few unused indexes (recently created / not yet exercised — don't drop pre-traffic), and the **Auth absolute-connection setting** (a Supabase **dashboard** toggle → percentage-based; user-driven).
- **Multi-currency — revenue-analytics rollup (follow-up).** The *charge* is currency-correct everywhere (negotiation zero-decimal + currency-at-creation now FIXED, `1f4a758`). The only remaining gap is cross-currency revenue *aggregation* in `lib/analytics.ts` (`getRevenueCents`/`getAgentDrivenRevenueCents`/`getPipelineCents` sum across currencies — single-currency workspaces correct, mixed = approximate; `currency` is on every `checkout_event` for a true per-currency breakdown). The Finance dashboard is already per-currency.
- **Magic-touch — DONE (`41ed58d`/`1bba97d`/`ce1ff29`/`21f8d77`).** The `/create` readiness "what's still missing" checklist shipped (`21f8d77`). Leftover only: an optional dedicated mobile bottom-nav (the `mobile` flag already orders the strip) + the long tail of 34 "medium" touches from the design sweep. See memory [[finance-and-magic-touch]].
- **Gating — `lib/ai-optimize` split RESOLVED (audited 2026-06-16):** already correctly split (`lib/ai-optimize.ts` is secret-free; LLM isolated in `lib/ai-optimize-llm.ts` w/ `server-only`); the fail-open-without-admin-env paths are intentional + safe in prod (service-role always set). No change needed. The one live gating gap the audit found is the outbound-webhook dispatch downgrade leak (see audit section). [[gating-review-followups]] [[absolute-worklist]]
- **Liquid Glass design-system rollout — effectively DONE (was a stale thread).** The global `globals.css` neutral remap (lines ~839-914) + the `lint:palette` enforcer already flip every hardcoded color in light mode; `PlatformFrame` routes pricing/discovery/simulator/leaderboard/support through `MarketingShell`; the dashboard shell satisfies the mobile nav rule; Cmd/K exists. No per-surface color work remains.
- **Eyeball-only (owner-credential gated):** the authed metrics-dashboard visual; forcing the 3c backlog alert into Better Stack (the alert path itself is verified live — backlog count + scheduled cron fire confirmed).

### Inspection data — DO NOT WIPE
**`qa-neg-sim-33`** (1 published page, **33 negotiations**) — the user's live 33-offer simulation; they want to log in and inspect it. All other `qa-*` smoke-test data has been cleaned up. (It legitimately carries `minPrice` in `rules`, which the `pages_public` view correctly strips for anon.)

### ⚠️ Critical workflow — parallel `main` (read before any code change)
The user (**Flyger1an**) + **Grok Build** + **codex** push to `main` **constantly, in parallel**. `origin/main` moves every few minutes, and the **local main checkout (`/Users/taio/nexez`) is usually their diverged WIP**. **Do NOT edit or commit in the main checkout.** Instead:
1. `git fetch origin -q` then `git worktree add -b <branch> /Users/taio/nexez-wt-X origin/main`.
2. For code changes: `rm -rf <wt>/node_modules` then `cp -al /Users/taio/nexez/node_modules <wt>/node_modules` — **Turbopack rejects a *symlinked* node_modules**; hardlink-copy works. `ln -s …/.env.local`. (Docs-only changes can skip node_modules.)
3. Edit only your files, verify (lint/tsc/test/build), `git push origin HEAD:main` (fast-forwards origin since you based on its tip). Then `git worktree remove <wt> --force` + `git branch -D`.
4. If a push doesn't trigger a Vercel build, re-trigger with an empty commit. **Env-var changes need a redeploy.** Commit trailer is flexible (`Grok Build`, `Claude Fable 5`, `Claude Opus 4.8` all used).

See memory: [[parallel-main-push-workflow]], [[nexez-deploy-verify-loop]].

---

## Project
**Nexez** helps businesses create dedicated, highly structured pages optimized for **AI agents** to discover, understand, and purchase from. Dual philosophy: **premium glassmorphism human dashboard** (now on **nexez.ai**) vs **brutally clean/semantic HTML for agents** on public `[slug]` pages (on **nexez.app**). A core objective is **deploying agent-optimized pages to custom domains**, managed from the Nexez backend.

## Stack & critical conventions
- **Next.js 16.2.7 (App Router, Turbopack)** — ⚠️ `AGENTS.md` warns this is a modified Next with breaking changes vs training data; **read `node_modules/next/dist/docs/` before using framework APIs**. The middleware file is **`proxy.ts`** at the repo root (exports `proxy`), NOT `middleware.ts` — it does custom-domain rewrites, the **.ai/.app canonical-host 308 split**, A/B bucketing, and the auth gate (`updateSession`).
- **Supabase** (Postgres + RLS + Auth + Storage). Clients: `utils/supabase/server.ts` (cookie/session), `utils/supabase/client.ts` (browser), `utils/supabase/admin.ts` → `createAdminClient()` / `hasSupabaseAdminEnv()` (service-role, gated). Project `pvsotrzgnjpqrsndhgmu`. **Anon reads of pages go through the `pages_public` view** (rules stripped); reads that need `rules` use the service-role client on base `pages`.
- **Deploy:** Vercel auto-deploys on push to `main`. **Three prod hosts, one app:** **`https://nexez.ai`** (marketing/discovery) + **`https://app.nexez.ai`** (the authenticated app — dashboard/login/auth/create) + **`https://nexez.app`** (the public agent runtime — `[slug]` pages + all agent artifacts + negotiate/checkout/webhooks/cron/v1). All serve the project directly; `proxy.ts` 308-canonicalizes each route to its host (`canonicalHostFor`). A shared `.nexez.ai` session cookie spans marketing+app; the agent runtime is cookie-isolated. Preview/branch URLs are SSO-gated. Vercel project `prj_uREcprpInoeHVhgV7aWc14TRsnLi`, team `team_rwkVXglmM5D0MPXjGTqOPVKO`.
- **Verify EVERY change:** `npm run lint -- --quiet` · `npx tsc --noEmit --incremental false` · `npm test` · `npm run build`. Route tests mock Supabase via `test/supabase-mock.ts` (`createSupabaseMock`); component tests declare `// @vitest-environment jsdom` + import `test/dom.ts`. Stripe-using route tests mock `stripe` with a **`class`**. E2E (`npm run test:e2e`) is opt-in; authed smoke runs only with `E2E_EMAIL`/`E2E_PASSWORD`.
- **Migrations:** apply via **Supabase MCP `apply_migration`** AND write the `.sql` into `supabase/migrations/`. **Never `supabase db push`.** All idempotent. The DB schema is authoritative via MCP regardless of how the migration *files* are organized (the user periodically runs `clean supabase migration history` commits).

## Credentials / account (repo is PUBLIC — never commit secrets)
- Test account **`realestglad@gmail.com`**, owner_id **`5320a9ef-9e9f-4e8b-ac78-13b9270c571b`**. Password is **not** stored — the user pastes it when authed verification is needed. **Clean up any test artifacts you create** (`qa-neg-sim-33` is the deliberate exception — leave it). If the user pastes a secret (e.g. `CRON_SECRET`) for a one-off, use it in-session only; never write it to a file.
- Safety boundaries shaping verification: **can't enter passwords/cards or move real money.** Owner-authed flows (escrow approve/capture/refund, completing the Pro subscription) stay **user-driven**. **Live mode is now active + validated** — the owner's Connect account `acct_1Tiny6IWDbJDFa4l` is linked + charges-enabled and one real escrow txn ran end-to-end (captured + refunded). The owner's own Pro subscription is genuinely `incomplete` (unpaid); negotiation/escrow is a Pro feature, so for the test it was temporarily flipped to `active` then restored — if you need Pro for a real test again, use that pattern (bump `published_page_grandfather.baseline` for a publish slot + flip `billing_subscriptions.status`, then RESTORE both + delete seeded data). See memory [[nexez-deploy-verify-loop]].

## Negotiation + escrow money system (the centerpiece — read for any money-path edit)
- **Lifecycle (`lib/negotiations.ts`):** statuses `negotiation → agreement_proposed → held → complete`, plus terminal `declined | expired | refunded | disputed`. `getAllowedNegotiationTransitions` / `canTransitionNegotiation`. `refunded`/`disputed` set only by the refund action / webhook.
- **Engine (`lib/negotiation.service.ts`, async since 3a):** `submitProposal()` (sync, on POST) records the buyer turn + marks the row `decision_pending`; **`runDecision()`** (async, from `after()` + the backstop cron) claims via a `decision_claimed_at` lease (exactly-once), runs the LLM with full history, **clamps to the rules floor (rules always win)**, persists turns to `negotiation_messages` (service-role), sets agreed `amount_cents`, classifies `settlement_state` (`lib/settlement.ts`), bumps `decision_seq`. **Token = credential** (`status_token`), enforced in `loadNegotiation`. Fallback decision on LLM failure so agents never hang.
- **Status poll (`/api/negotiations/status`):** returns `decisionPending`/`decisionSeq` + the sanitized decision (`lib/negotiation-sanitize.ts` strips `internalNotes`) + `settlementState`/`payable`.
- **Buyer pays (`app/api/negotiations/pay/route.ts`):** token-gated; Connect-routed Checkout (`auto`→immediate capture, `approved`→manual hold); idempotent; dual-mode (JSON for agents, 303 for the `/negotiate` form).
- **Owner actions (`app/api/negotiations/escrow/route.ts`):** `approve | capture | cancel | refund` (Connect-aware via `billing_subscriptions.stripe_connect_account_id`). State changes go through `/api/negotiations/transition` (`canTransitionNegotiation`), not the browser. **Both escrow + transition are served on `APP_HOST`** (they're dashboard/session actions, in `APP_API_PREFIXES`) — NOT the agent runtime, else the dashboard POST hits a cross-origin 308.
- **Webhook (`app/api/webhooks/stripe/route.ts`):** **multi-secret** — verifies against `STRIPE_WEBHOOK_SECRET` (your-account endpoint) AND `STRIPE_WEBHOOK_SECRET_CONNECT` (connected-accounts endpoint), since the escrow charge fires as a **connected-account** event from a second Stripe endpoint with its own secret. HMAC-verified first; event-id idempotency ledger (`stripe_webhook_events`, service-role-only); settle + reversal events; self-heals.
- **Crons:** `reconcile-escrow` (hourly — Stripe↔DB drift; now **backfills** paid Checkout sessions that have no stored PI, so a missed webhook can't strand money) + `process-negotiations` (`*/5` — backstop the async decision + aggregate backlog alert). Both `CRON_SECRET`-gated. Manual trigger: `curl -H "Authorization: Bearer $CRON_SECRET" https://nexez.app/api/cron/reconcile-escrow`.
- **DB safety:** trigger `nz_negotiation_money_safety` blocks `held`-without-PI and `complete`-of-a-payment-backed-neg-without-a-captured-hold (unless `auto`/`disputed`).

## Architecture notes for common edits
- **3-host split:** `lib/site.ts` (host helpers — `MARKETING_HOST`/`APP_HOST`/`AGENT_RUNTIME_HOST` + `canonicalHostFor`/`appUrl`/`marketingUrl`/`agentRuntimeUrl`), `proxy.ts` (canonical 308s + custom-domain rewrite + auth gate), `utils/supabase/cookie-options.ts` (`getSupabaseCookieOptions` → shared `.nexez.ai` cookie for marketing+app, cookie-isolated elsewhere), `components/MarketingShell.tsx` + `components/PlatformFrame.tsx` (chrome routing: marketing→MarketingShell, `/dashboard`+`/create`→PlatformShell, else bare), host-aware `app/sitemap.ts` + `app/robots.ts`. `getBaseUrl()` (`lib/agent-page.ts`) = nexez.app and must stay (agent contract).
- **Core types/helpers:** `lib/agent-page.ts` — `OfferItem`, `OfferRules` (incl. `autoSettleMax`), `AgentPage`, `PUBLIC_PAGE_SELECT`/`OWNER_PAGE_SELECT`, `getCheckoutOffer`, `getRequestBaseUrl`, readiness/trust/certification.
- **Agent artifacts:** `lib/agent-manifest.ts`, `lib/mcp-server.ts`, `app/[slug]/{agent.json,mcp.json,mcp,llms.txt,badge.svg,badge.json}` (all on nexez.app).
- **Stripe Connect / commission:** `lib/stripe-billing.ts`; `app/api/checkout/route.ts` is the canonical connected-account + `application_fee` pattern (reused by `pay`). **A charge now requires the seller's Connect account** — no Connect → external-link fallback / 409, never a platform-account charge.
- **Feature gating (the monetization layer):** `lib/billing.ts` is the single-source catalog (`PlanId`/rank/limits/commission ladder/`FEATURE_MIN_RANK`/`planAllows`/`getPlanLimits`/`minPlanForFeature`). Server resolution: **`lib/server/plan.ts`** `getOwnerPlanId(supabase, ownerId)` (status-aware — `LIVE_STATUSES` only, else Free) + `ownerAllows(...)`. UI: `components/billing/{PlanGate,UpgradeBanner,ProBadge}` (teasers, Enterprise→/support) + `PlanProvider`/`usePlan()` (set in `app/dashboard/layout.tsx`). **Server enforcement lives on the consuming route/render, not the owner's write** (client writes are RLS-only → bypassable). Hard chokepoints are **DB triggers**: `trg_enforce_published_page_limit` (grandfather baseline table `published_page_grandfather`) + `trg_enforce_team_collaboration` (via `owner_plan_rank()`). Agent manifests gate negotiation via `lib/server/negotiation-visibility.ts` `resolveNegotiationAllowed`.
- **Multi-currency:** `pages.currency` (settlement currency; default `usd`); `lib/currency.ts` (`normalizeCurrency`/`toStripeAmount` **zero-decimal-safe**/`formatCurrencyAmount`/`toMajorAmount`). Checkout charges in the page currency. **`currency` MUST be in both `PUBLIC_PAGE_SELECT` AND the `pages_public` view** (the SEV1 coupling).
- **Finance dashboard:** `app/dashboard/finance/{page,RevenueChart}.tsx`; pure roll-ups in **`lib/finance-analytics.ts`** (`rollupFinanceByCurrency`/`getDailyRevenueSeries`/`getTopOffersByRevenueCents`/`getCurrencyOptions` — never sum across currencies); live payouts via server-only `lib/server/connect-finance.ts` (`getConnectPayoutSnapshot`). Full history Pro-gated like Analytics.
- **Observability:** `lib/observability.ts` `captureError` (gated on `OBSERVABILITY_WEBHOOK_URL`; `OBSERVABILITY_WEBHOOK_TOKEN` → Bearer; warns on non-2xx). Callers: reconcile + process crons, negotiation persist failures.
- **Custom domains:** `lib/custom-domain.ts` (`isPlatformHost` now also recognizes nexez.ai), `proxy.ts`, `lib/vercel-domains.ts` (gated), `app/api/custom-domain`.
- **App shell:** root `app/layout.tsx` → `PlatformFrame.tsx` → `PlatformShell` (app) | `MarketingShell` (marketing) | bare. Dashboard `app/dashboard/page.tsx` is a server component → `DashboardClient.tsx`. Editor `/dashboard/[id]` is a server component → `EditorClient`.
- **Email:** `lib/email.ts`; `EMAIL_FROM` overridden to the Resend-verified `updates.nexez.app` sender.

## Design system — Liquid Glass (bridge-first integration, COMPLETE)
- **Source-of-record (committed `360443c`, untracked working copies live in the main checkout):** `DESIGN_SYSTEM.md` (the full spec + the **mobile doctrine**), `nexez-design-system.css` + `nexez-fx.js` (the canonical CSS/motion reference), `nexez-logomark.svg`, `Reference/nexez-1..7.html` (7 visual reference pages — *reference only, not shipped*).
- **How it's wired into the app (NOT a drop-in of the reference files):** a **"Liquid Glass bridge" appended to the end of `app/globals.css`** (cascade-wins; coexists with the existing theme system). Tokens are keyed on **`html.light`** — the app's real theme toggle is `html.light`/`html.dark` (default dark), **NOT `data-theme`** (`lib/theme.ts` + `ThemeToggle`). The `.public-agent-page` scope re-declares tokens dark so agent pages never flip. Tailwind v4 (`@import "tailwindcss"`, no `@theme` block) — colors resolve via CSS vars.
- **Motion** is React, not the raw `nexez-fx.js`: **`components/DesignSystemFx.tsx`** (mounted in `app/layout.tsx`) does cursor specular + scroll reveals + KPI counters + ring draw under `html.fx`; it **omits** the h1 word-stagger + the `cloneNode` mobile-nav (those fight React — the mobile menu is rebuilt in React per shell). `NexezLogo` is now an inline prism-diagonal SVG N (`currentColor` + a periwinkle→teal→amber gradient), same `className` API.
- **⚠️ Token gotchas (bitten before):** the app's **`--muted` is a *background* color (`#18181B`), not muted text** — use `--fg`/`--fg-muted`/`--fg-muted-2` for de-emphasized text. Brand accent shifted **purple → periwinkle `--signal`** (#8E9EFF dark / #5566F2 light); also `--ready` (teal), `--amber`, `--prism`, `--glass*`, `--btn-grad`/`--pri-grad`. New vocabulary: `.btn`/`.btn-primary`/`.btn-secondary`/`.btn-sm`, `nav.glassnav`/`.navlinks`/`.navsheet`, `.glass`/`.glass.lift`/`.glass.prism`, `.chip`/`.eyebrow`/`.display`/`.lede`/`.wrap`/`.kpi-num`/`.grad-text`/`.hair`.
- **HARD mobile rule — "never ship a breakpoint where navigation disappears":** links may hide **only** under `.has-menu` (added by React/JS *after* hydration); the no-JS baseline keeps the link row visible as a horizontal scrollable strip. ☰ opens an **opaque** `.navsheet` (Escape + outside-click close); Sign-in folds into the sheet ≤480px. Applies to every shell (marketing `.glassnav` + the app `PlatformShell` — the dashboard rail is a fixed bottom nav on mobile, always visible — both satisfy this).
- **Edit-tool gotcha:** multibyte chars (em-dash `—`, the `☰`/`☰` glyph) break `old_string` matching — anchor edits on ASCII-only substrings.

## Gated features (env-set; **Vercel → project `nexez` → Settings → Environment Variables → Production**, then redeploy)
| Env var(s) | Unlocks | Status |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` + `…PUBLISHABLE_KEY` | the app runs | ✅ set |
| `SUPABASE_SERVICE_ROLE_KEY` | `/api/v1/*`, account export/delete, webhook + cron + negotiation DB writes, the `pages_public`-bypassing rules reads | ✅ set |
| `STRIPE_SECRET_KEY` (+ `STRIPE_PRICE_*`) | billing subs, hosted checkout, **all negotiation escrow** | ✅ set (**`sk_live`** — LIVE). ⚠️ confirm `STRIPE_PRICE_*` are **live** price IDs (test IDs while live = create-subscription 500) |
| `STRIPE_WEBHOOK_SECRET` | the **"your account"** webhook endpoint (billing/subscription sync) | ✅ set (live) |
| `STRIPE_WEBHOOK_SECRET_CONNECT` | the **"connected accounts"** webhook endpoint — escrow capture/refund/dispute events (a SEPARATE Stripe endpoint with its OWN secret; the handler verifies both) | ✅ set (live) — without it, escrow events don't sync |
| `LLM_API_KEY` (+`LLM_BASE_URL`/`LLM_MODEL`/`LLM_PROVIDER`) | real LLM negotiation + assist (grok-4.3) | ✅ set |
| `CRON_SECRET` | gates `/api/cron/{freshness,reconcile-escrow,process-negotiations}` (Vercel cron auto-auths) | ✅ set |
| `RESEND_API_KEY` (+`EMAIL_FROM`) | transactional email | ✅ set (`updates.nexez.app`; delivered) |
| `OBSERVABILITY_WEBHOOK_URL` (+`OBSERVABILITY_WEBHOOK_TOKEN`) | `captureError` → Better Stack | ✅ set (verified) |
| `AGENT_VISIT_HASH_SALT` | salt for agent-visit IP hashing | ✅ set (don't rotate) |
| `VERCEL_API_TOKEN` + `VERCEL_PROJECT_ID` + `VERCEL_TEAM_ID` | auto custom-domain attach + SSL | ✅ set |
| `NEXT_PUBLIC_SITE_URL` | **load-bearing** — feeds `AGENT_RUNTIME_HOST` + `getBaseUrl()` (agent artifacts/escrow links). **Must stay `https://nexez.app`** (NOT app.nexez.ai) | ✅ `https://nexez.app` |
| `NEXT_PUBLIC_MARKETING_URL` | the **marketing** host (`MARKETING_HOST`) | ⬜ optional (code defaults to `https://nexez.ai`) |
| `NEXT_PUBLIC_APP_URL` | the **authenticated app** host (`APP_HOST`) — dashboard/login/auth | ⬜ optional (defaults `https://app.nexez.ai`); **must be a subdomain of the cookie domain** |
| `NEXT_PUBLIC_AGENT_RUNTIME_URL` | overrides the **agent runtime** host (`AGENT_RUNTIME_HOST`); takes precedence over `SITE_URL` for `getBaseUrl()` | ⬜ optional (falls back to `SITE_URL` → `nexez.app`) |
| `NEXT_PUBLIC_AUTH_COOKIE_DOMAIN` | shared session-cookie domain across marketing+app | ⬜ optional (defaults `.nexez.ai`); must cover `app.nexez.ai` |

> **3-host architecture (commit `669c05d`):** `nexez.ai` marketing · `app.nexez.ai` authenticated app · `nexez.app` public agent runtime. A shared `.nexez.ai` session cookie spans marketing+app (agent runtime is cookie-isolated). **Cutover prereqs:** attach `app.nexez.ai` in Vercel (DNS+SSL); set Supabase Auth Site URL=`https://app.nexez.ai` + Redirect URLs `https://app.nexez.ai/**` (login moved there).

**NOT env vars:** Square/Acuity/GCal/Calendly/Shopify creds + Stripe *import* are per-request. Calendly webhook uses a per-page secret in `page_secrets`.

## Dev / verification workflow notes
- **Prod is the verification surface** for Stripe / service-role / LLM — `.env.local` has Stripe keys but **no** `SUPABASE_SERVICE_ROLE_KEY` and **no** `LLM_API_KEY`, so the negotiation service + real LLM only work on **nexez.app**. Local dev (`npm run dev`) reads from prod Supabase via the anon key (good for testing routing/public pages).
- **Middleware/routing can be tested locally** by spoofing `Host`: e.g. `curl -H "Host: nexez.app" http://127.0.0.1:3100/pricing` → 308 to nexez.ai (the proxy only canonicalizes the three prod hosts; localhost/preview serve everything).
- **Signed-webhook trick** (escrow/reversal without a browser): HMAC-SHA256 over `${ts}.${payload}` with `STRIPE_WEBHOOK_SECRET`, header `stripe-signature: t=<ts>,v1=<sig>`, POST to `/api/webhooks/stripe`.
- **DB writes via Supabase MCP `execute_sql`** (service-role) for seeding/cleanup; **always clean up** test data (delete pages/negotiations/messages/visits/events). Use future-dated `decision_requested_at` if seeding pending rows you don't want the cron to process.
- **Shell quirk:** the Bash tool occasionally loses `PATH` mid-loop (`curl: command not found`) — prefix with `export PATH="/usr/bin:/bin:/usr/local/bin:/opt/homebrew/bin"` and/or use absolute `/usr/bin/curl` for multi-step probes.

## Quick verification commands
```bash
git fetch origin -q && git rev-list --left-right --count origin/main...main   # left=origin-only right=local-only
npm run lint -- --quiet && npx tsc --noEmit --incremental false && npm test && npm run build
```

## Earlier history (pre-this-session, see git log)
Smart Rules Phase 1+2 (per-offer fixed/negotiable + rules engine + intelligent negotiation engine with multi-provider LLM adapters + persistent `negotiation_messages` threads), dual-revenue Stripe billing (subs for paid plans via Nexez MoR + Connect application-fee commissions), 5-tab glassmorphic `/dashboard/billing`, `/pricing`, `/onboard`, deeper LLM surfaces (`/api/simulate-llm`, `/api/trust-report`, AICoPilot), importer/Visual Builder/analytics, 6 integrations, custom-domain hosting, MCP JSON-RPC, trust/badges/certification, marketplace, leaderboard, `/api/v1/*` + keys, account export/delete, team RLS, rate limiting, freshness cron, theming. CI (`.github/workflows/ci.yml`) gates lint/tsc/test/build.
