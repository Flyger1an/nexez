# Nexez Roadmap

**Mission:** Build the category-defining platform for **agent-optimized pages** — lightweight, crawlable, bookable surfaces that AI agents discover, understand, and transact with, while staying beautiful and linkable back to the human website. Dual philosophy throughout: **premium glassmorphism for humans, brutally clean semantic HTML for agents.** Supports professional services *and* consumer/local bookable services (plumbing, massage, cleaning, fitness, grooming, detailing).

> This is the product roadmap (vision → shipped → pending). For session/operational state (HEAD, env vars, verification commands, dev quirks) see [HANDOFF.md](HANDOFF.md). The full implementation history lives in git log, not here — keep this file concise.

## Quality bars (definition of done)
- **Importer magic** — paste a site → rich editable offer cards in ~30s; lossless import → builder → publish roundtrip; industry-aware defaults.
- **Builder fidelity** — VisualOfferBuilder is the single source of truth; 100% of `OfferItem` fields (tiers, duration, serviceArea, isMobile, travelFee, url, per-offer flags) roundtrip losslessly to DB and public page.
- **Analytics as ROI proof** — a new user sees evidence that agents are discovering and driving value in <5 min (charts, funnel, agent breakdown, per-offer conversion, pipeline $). Exportable.
- **Discovery power** — best-in-class directory/marketplace for agents + humans: pro/consumer split, facets, "agents also viewed", readiness/trust signals, embeddable cards.
- **Integrations "set once, forget"** — real sync + outbound webhooks for agent-driven bookings; per-integration status UI.
- **Agent consumption supremacy** — schema.org/JSON-LD + llms.txt + agent.json + MCP + plain-text on every published page; zero hallucination risk; public pages load fast and crawl cleanly (GPTBot/Claude/Perplexity/Grok…).
- **Production hardness** — test coverage on critical paths, error boundaries, working custom-domain verification, audited RLS/auth, clean build + typecheck, monitoring hooks.
- **Dual philosophy preserved** — every screen keeps the human-premium / agent-clean split. No bloat that complicates the clean agent HTML or the premium creation flow.

## Shipped (Phases 1–8 + Phase 7 advanced tiers — all live)
- **Importer & Builder** — multi-path crawl + schema.org/JSON-LD extraction returning rich `OfferItem[]` (confidence/source); guided import review with clarifying-answer refinement, 125+ industry/niche autocomplete, provenance, readiness, offer caps, and honest AI-status telemetry (`aiStatus`: used / fallback / deterministic); VisualOfferBuilder with tiers, consumer fields, templates, drag; create wizard + editor share the rich surface; intelligent re-sync with smart merge.
- **Analytics** — full Recharts suite (traffic, top offers, funnel, action/agent breakdown, top pages, conversion leaders), time ranges, key-insights, range-aware export. Real agent-visit tracking + agent-vs-human detection.
- **Directory / Marketplace** — pro/consumer split, facets (offer type, category, readiness thresholds), "agents also viewed", readiness/trust badges, favorites, click tracking, public `/api/directory` for agents.
- **Integrations** — Calendly, Stripe, Shopify, Square, Acuity, Google Calendar (import → editable offers, re-sync, status pills); outbound webhooks (per-page endpoints + secrets, fire on real booking/checkout events); Calendly + Stripe webhook receivers.
- **Per-offer controls & embeds** — per-offer "book on original site" flag (roundtrips via pipe-safe markers; respected in public CTAs, JSON-LD, agent.json); embed generator (iframe + real `/widget.js` floating button); version history (JSONB snapshots + restore).
- **Custom-domain hosting (Phase 8, core objective)** — host→serve via `proxy.ts`, managed SSL provisioning, connection wizard, brand-root `agent.json`/`mcp.json`/`llms.txt`, multi-page domains, white-label branding + inheritance, draft→preview→publish staging, deployments/rollback, real DNS-TXT verification.
- **Agent artifacts** — JSON-LD, llms.txt, `agent.json`, OpenAPI, plain-text context, badge SVG/JSON, and a **real MCP JSON-RPC endpoint** (`/[slug]/mcp`) + `mcp.json`.
- **Phase 7 advanced** — global Simulator + per-page history (fully functional with LLM insights), AI Co-Pilot (full LLM-powered descriptions/pricing/FAQ/schema with before-after + one-click apply using the platform's configured LLM), Trust Score + verification (advanced composite + signals), competitor intelligence (LLM-enhanced analyzer), agent memory/context (LLM-suggested persistent notes), voice optimization (LLM-powered phonetic), team collaboration + approvals (functional workflows). Deeper LLM (NEW /api/simulate-llm for naturalLanguage agent responses in simulator "LLM-Enhanced" tab when page llm_opt_in, /api/trust-report, PDF exports in simulator/analytics, auto LLM memory on import, proposal LLM review for negotiations, async LLM in voice/competitor/manifest, llm_opt_in settings toggle + suggest, dedicated seeded E2E, generalized to any OpenAI-compat provider with no engine hardcode).
- **Commerce & trust** — negotiations end-to-end + receipts, gated email on new negotiation, escrow scaffolding, leaderboard, programmatic **API + keys** (`/api/v1/*`), account export/delete, team invites + collaborator RLS, in-app notifications, rate limiting, observability hook, freshness-monitor cron.
- **Dual-revenue billing + Stripe Connect** — Stripe **subscription billing** only for paid plans (Launch/Pro/Scale/Enterprise) via Nexez as merchant (`/dashboard/billing`, `/api/billing/{checkout,portal}`, plan Price IDs; gated → "setup Stripe" redirect when unconfigured); **Stripe Connect (Express) + Application Fees** for transactions on ALL plans (incl. Free — no sub fee but commission applies) with owner as MoR and Nexez taking configurable % per plan (15% Free, 8% Launch/Pro, 6% Scale, 4% Enterprise) automatically. New Connect onboarding flow in /onboard + `/api/billing/connect`; checkout updated to sessions on connected account with `application_fee_amount`; billing UI + pricing show separate sub vs. platform fee views + commissions. `lib/billing.ts` + `lib/stripe-billing.ts` with helpers. Migration `20260615120000_extend_billing_for_connect_and_commission.sql` (adds connect fields + commission_percent to billing_subscriptions; fixes plan check). Onboard + dashboard/integrations/billing updated. Persistent negotiations (dedicated `negotiation_messages` table + `/negotiate/{id}` page with full history + LLM context + owner manual UI). All Stripe surfaces degrade gracefully when `STRIPE_SECRET_KEY` is unset. Support desk (`/support`, `/api/support/{tickets,assist}`; tickets persist to `support_tickets`); **Nexez-hosted checkout** (`/checkout/[slug]` → real Stripe Checkout Session on owner's Connect when available, with provider-redirect fallback).
- **Platform polish** — premium shell (collapsible nav, search), dedicated Pages section with bulk actions + duplicate, redesigned homepage, bolder Nexez brand mark, platform-wide light/dark/system theme, hardened auth UX, onboarding checklist + empty states, real A/B variant serving + attribution.
- **Editor architecture** — the page editor (`app/dashboard/[id]`) is a server component (auth + parallel fetch + access redirects) feeding an `EditorClient` island over a `usePageEditor` hook + presentational panels (`components/editor/*`); the smart-merge / save-payload / version logic lives in unit-tested `lib/editor-merge.ts`.
- **Billing, support & hosted checkout** — Stripe **subscription billing** (`/dashboard/billing`, `/api/billing/{checkout,portal}`, plan Price IDs; gated → "setup Stripe" redirect when unconfigured); **support desk** (`/support`, `/api/support/{tickets,assist}`; tickets persist to `support_tickets`); **Nexez-hosted checkout** (`/checkout/[slug]` → real Stripe Checkout Session, with provider-redirect fallback for offers that prefer the original site). All Stripe surfaces degrade gracefully when `STRIPE_SECRET_KEY` is unset.
- **Security/RLS hardening** — Supabase Security Advisor is clean (0 errors / 0 warnings / 0 info suggestions); leaked-password protection is enabled in Auth Attack Protection. Scoped the `logos` bucket (dropped broad listing), moved `nz_page_is_published` to a non-PostgREST `private` schema (RPC no longer exposed; negotiation INSERT policy intact), indexed the `support_tickets` FK; collaborator RLS policies already use the `(select auth.*())` init-plan form.
- **Test coverage** — beyond the `lib/` unit suite (~70 files / 372 tests): **API route-handler tests** (vitest, mocked Supabase via `test/supabase-mock.ts`) covering auth/tenancy/gating across api/v1 (+`[id]`), negotiations, account delete/export, custom-domain verify, Stripe webhook, billing checkout, public-simulate, simulate-llm/trust-report paths, and the `agent.json`/`llms.txt` artifacts; **component tests** (jsdom + Testing Library: ThemeToggle, ErrorBoundary, VisualOfferBuilder, PageCard); and **Playwright E2E** (`npm run test:e2e`) — public-page smoke (incl. stabilized LLM-Enhanced tab + new dedicated "simulator LLM-Enhanced (seeded llm_opt_in page)" test: auth → settings toggle seeds llm_opt_in → /simulator paste → asserts real /api/simulate-llm + naturalLanguage) + authed editor smoke (creds via env, skips in CI). Responsive layout verified at mobile across all routes (no horizontal overflow). Live curls on dev instance with key verified the new LLM routes.

- **Smart Rules + hybrid booking (Phase 1)** — per-offer **Fixed vs Negotiable** types with an owner rules engine (`lib/offer-rules.ts`): min price / max discount / auto-accept (+band), min notice, blackout dates, weekly booking cap. Negotiable offers get a public **"Make an Offer"** CTA + structured proposal form; proposals are evaluated server-side — in-rules auto-accepts straight to `agreement_proposed`, below-minimum is flagged for the inbox (badges). Agents get a token-gated **status endpoint** (`GET /api/negotiations/status`) advertised in `agent.json`/`mcp.json` alongside public-safe `booking_constraints` (pricing rules stay owner-private, test-asserted). Checkout enforces the weekly cap + blackouts. `OfferItem` roundtrip fidelity via `[[TYPE]]`/`[[RULES]]` markers; re-import merge preserves rules.

## Pending / next
Forward-looking work, roughly by leverage:
- **Smart Rules Phase 2** — LLM proposal review + counter-offer suggestions (plugs into the existing `RulesEvaluation`), scope rules (inclusions/revisions/length), richer agent status updates, and Calendly write-side calendar blocking (per-page API tokens + slot picking).
- **Email reach** — booking emails ship for the Calendly webhook (`lib/email.ts` `buildBookingEmail`, gated on `RESEND_API_KEY`); extend to the Stripe webhook and add per-user notification preferences; consider account email via service-role lookup.
- **Stripe production readiness** — configure `STRIPE_SECRET_KEY`, plan Price IDs, and `STRIPE_WEBHOOK_SECRET`; then verify billing, hosted checkout, escrow, and price-sync flows against real Stripe events (Connect + app fees now live for txns).
- **Real LLM assist** — Core advanced LLM now shipped (see Shipped Phase 7 + new routes /simulate-llm + /trust-report, simulator depth + naturalLanguage when llm_opt_in, PDF, auto on import, proposal review, generalized platform LLM, settings toggle + suggest, seeded E2E). Still gated on `LLM_API_KEY` for prod power; importer telemetry + basic opt-in plumbing was prior. Further prompt tuning / more surfaces / prod keys ongoing.
- **Deeper integrations** — real bidirectional Calendly/Stripe sync (availability reflected in agent pages), richer price webhooks; per-domain scoped `llms.txt`/`openapi.json` (currently global).
- **Launch-prep differentiators** — templates marketplace (curated + user packs), "Nexez Certified Agent-Ready" certification, seed 20–30 high-quality directory pages, comparison/case-study marketing pages.
- **Deeper test coverage / CI** — GitHub Actions CI is live (`.github/workflows/ci.yml` push/PR gate; `e2e.yml` manual Playwright) and repo secrets are configured. Next: keep extending route/component suites into the long tail (~25 remaining API routes, more dashboard components) and run manual E2E before major releases.
- **Housekeeping** — light-mode long-tail (rare hardcoded neutral classes on deep screens) and docs freshness after each shipped roadmap slice.

## Governance
- Preserve the human-premium / agent-clean split on every change. No bloat to public agent HTML or manifests.
- `OfferItem` is the sacred data model — any serialization change ships with a roundtrip test; prefer pipe-safe markers / JSONB over schema churn.
- DB migrations: additive, idempotent, applied via Supabase MCP `apply_migration` + committed `.sql`. **Never `supabase db push`** (tracking-table versions differ from repo filenames).
- Verify every change: `npm run lint -- --quiet` · `npx tsc --noEmit --incremental false` · `npm test` · `npm run build`.
- Record granular work in commit messages and HANDOFF.md, not in this file — update the Shipped/Pending sections only when scope actually changes.

## Platform Audit Snapshot (2026-06-15, post billing overhaul + clean; HEAD `0b129c1`)
**Verification run (pre-commit):** uncommitted changes include billing/Connect/checkout/onboard updates + new migration + doc syncs (plus prior); `npm run lint -- --quiet && npx tsc --noEmit --incremental false && npm test && npm run build` will be run clean before `git add + commit`. `npm test` (~71 files / **379 tests passing** — includes billing/Connect paths, negotiation engine + messages table, e2e, etc.). E2E public + editor smoke green in local runs (with creds for authed). Latest prior commit: negotiation engine feat.

**Code evidence of this batch (billing overhaul: dual revenue + Stripe Connect + persistent negotiations) + prior:**
- NEW migration `20260615120000_extend_billing_for_connect_and_commission.sql`: adds stripe_connect_* + commission_percent to billing_subscriptions; fixes plan_id check for free/enterprise.
- `lib/billing.ts`: added commissionPercent per plan (Free 15%, Launch/Pro 8%, Scale 6%, Enterprise 4%).
- `lib/stripe-billing.ts`: added createStripeConnectAccount, createStripeConnectOnboardingLink, getCommissionPercentForPlan, calculateApplicationFeeCents.
- NEW `app/api/billing/connect/route.ts`: creates Connect Express account + onboarding link; stores in billing_subscriptions.
- `app/api/checkout/route.ts`: now creates sessions on owner's connected account (if present) with application_fee_amount based on plan commission.
- `app/onboard/page.tsx`: updated step 3 to trigger Connect onboarding (for txns) separate from subs.
- `app/dashboard/billing/page.tsx`: enhanced with Connect section, accurate per-plan fee explanation (separate from subs).
- `app/pricing/page.tsx`: shows commissions per tier.
- Persistent negotiations: dedicated `negotiation_messages` table (prior migrations 20260614120000 + 20260614120100) + `/negotiate/{id}` page (full history from table) + service using LLM adapters + owner manual UI in dashboard/negotiations.
- Prior billing/pricing/onboard/LLM + Smart Rules evidence (as before). Full verify + 379 tests post-changes.

**Comparison to Roadmap "Shipped" (Phases 1–8 + Phase 7 tiers + polish) — updated for advanced LLM batch:**
- **Matches/exceeds almost verbatim**: Importer & Builder (multi-path, rich OfferItem + confidence/source + AI telemetry/guided + re-sync/smart-merge + industry + lossless); Analytics (Recharts full + time-range + key-insights + agent-driven + page-scoped + unserved-query gap + export); Directory/Marketplace (pro/consumer, facets/readiness/thresholds, "agents also viewed", trust/verified/creds badges, favs/persistent, click tracking, /api/directory); Integrations (Calendly/Stripe/Shopify/Square/Acuity/GCal with live-API for several + re-sync + status + outbound webhooks + receivers firing real events); Per-offer/embeds/versioning + custom-domain (Phase 8: proxy.ts + SSL + wizard + multi-page + white-label/branding + inheritance + logos storage + draft-preview-publish + DNS-TXT + deployments/rollback); Agent artifacts (JSON-LD/llms/agent.json/OpenAPI/plain-text + **real MCP JSON-RPC** `mcp.json` + /mcp + badge.svg/json); Phase 7 advanced (global + per-page Simulator w/ history/export + query-aware, AI Co-Pilot w/ tabs+before-after+apply incl. Voice, Trust Score+verification+chips+badges+events, Competitor Analyzer early, verifiable creds, agent memory/context, voice opt, team collab+approvals); Commerce & trust (negotiations end-to-end + receipts + gated email + Stripe escrow/manual-capture, leaderboard, API+keys /api/v1, account export/delete, team invites+RLS, notifications, rate-limit, observability, freshness cron); Platform polish (premium shell + search + responsive mobile no-overflow, dedicated Pages w/ bulk/duplicate, redesigned homepage, bolder brand, light/dark/system, hardened auth/onboarding/empty-states, real A/B); Editor architecture (server component + EditorClient + usePageEditor + presentational panels + unit-tested lib/editor-merge); Billing/support/hosted checkout (Stripe subs + /billing + portal/checkout routes + gated graceful degrade; support desk + tickets; Nexez-hosted /checkout/[slug] + Stripe Session + provider fallback); Security/RLS (advisor 0/0/0, leaked-pw enabled, logos bucket scoped, private schema for nz_page_is_published, collaborator RLS init-plan, 29+ migrations applied); Test coverage (lib  + API route-handler mocks + jsdom components + Playwright E2E public+authed-editor + CI gates + 333 tests/67 files + responsive verified).
- **Recent user-requested (logo files + remove + one-click)**: Fully shipped and integrated (file upload + remove in settings + create, one-click importer detect + auto-fill on site import, passed through drafts/branding, preview on public page). Aligns with "white-label branding + inheritance" + "logos storage" in Shipped.
- **Governance/verify**: All changes follow (lint/tsc/test/build run in this audit + prior; additive migrations; OfferItem fidelity preserved via tests; dual philosophy held; granular commits + HANDOFF/ROADMAP updates).

**Pending items (per Roadmap) — current status vs. list:**
- Email reach: Calendly booking emails shipped (gated RESEND_API_KEY per lib/email + webhook); Stripe extension + per-user prefs **not yet** (scaffolding exists).
- Stripe prod readiness: Scaffolding + tests + graceful degrade (when unset shows "setup" in billing) + subs persist + escrow + price-sync + webhooks **excellent**; Connect + app fees for txns now live (real keys/webhook still needed for prod; many tests stub 'sk_test_ready').
- Real LLM assist: **Core advanced shipped** (see evidence + updated Shipped Phase 7: simulate-llm/trust routes, simulator depth, PDF, auto-import, proposal review, llm_opt_in E2E, generalize, settings suggest, more tabs, async paths). Still gated on `LLM_API_KEY` + per-page llm_opt_in for prod power; further expansion (more surfaces, prod keys, Smart Rules LLM counters) pending.
- Deeper integrations / launch-prep / housekeeping: as prior (CI/e2e in place and extended; directory strong; etc.).
- Deeper test/CI: extended with LLM routes + seeded E2E + billing/Connect paths; still room on long-tail routes.

**Audit summary / standing vs. quality bars & roadmap:**
- **Extremely strong / production-ready on most dimensions** (importer + fidelity + analytics + discovery + integrations + agent supremacy + production hardness + dual philosophy all met or exceeded; **379 tests**, clean security advisor, live custom domains, real MCP, billing+checkout+support + dual revenue/Connect, extensive LLM depth + polish).
- Completion against full "Shipped" list in ROADMAP: **~99%+** (core + Phase 7/8 + Smart Rules P1 + advanced LLM depth + dual billing/Connect + persistent negotiations + polish live; minor env-gates + deeper polish/expansion pending as listed).
- Gaps are mostly **env-gated or future-launch** (not core functionality); no hard breakages (build/tests/lint/e2e/curls clean; "input changes" for deeper LLM + billing overhaul fully wired + tested + generalized + documented).
- Modularity/flywheel preserved (JSONB memory/simulations, importer for LLM, events for trust/revenue, rules+LLM hybrid in negotiations; separate sub vs. txn revenue streams).
- Recommendation: Smoke + commit + deploy this batch (billing overhaul + MD sync); next on pending (emails/Stripe prod keys, Smart Rules P2 full, more LLM surfaces, templates/seed). All prior fidelity/dual-philosophy preserved. Platform standing "launch-prep / category-defining".

(End of audit snapshot. ROADMAP "Shipped/Pending" remain the source of truth; this is a point-in-time verification after incorporating the latest user input on billing dual model + docs clean/sync.)
